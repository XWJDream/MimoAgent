name: Build & Test

on:
  push:
    branches: [dev, master]
  pull_request:
    branches: [dev]

jobs:
  build:
    runs-on: windows-latest

    strategy:
      matrix:
        node-version: [18, 20]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: |
          npm install
          cd engine && npm install

      - name: Build engine
        run: npm run build:engine

      - name: TypeScript check
        run: npx tsc --noEmit

      - name: Build renderer
        run: npx vite build

      - name: Build main & preload
        run: |
          npx tsc -p tsconfig.main.json
          npx tsc -p tsconfig.preload.json

      - name: Verify no secrets in build
        run: |
          echo "Checking for API keys..."
          if grep -r "sk-[a-zA-Z0-9]" dist/ engine/dist/ 2>/dev/null; then
            echo "ERROR: API key found in build output!"
            exit 1
          fi
          echo "✓ No secrets found"

      - name: Upload build artifacts
        if: matrix.node-version == 20 && github.ref == 'refs/heads/dev'
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: dist/
          retention-days: 7
